person = {
    name: "Muskan Agarwal Bosamiya",
    age: 20,
    coutry: "Manthan's heart"   
}

function logData(){
    let toBePrinted = person.name + " is " + person.age + " years old " + "and lives in " + person.coutry
    return toBePrinted
}

console.log(logData())